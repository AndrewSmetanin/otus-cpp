#travis configuration for coverage testing
language: cpp
# Compiler selection
compiler: gcc

os: linux

deploy:
  provider: bintray
  file: "descriptor.json"
  user: "Bintray user"
  key: "Bintray API key"